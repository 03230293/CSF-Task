user_inputs=[]
for i in range(3):
        user_input=int(input())
        
def is_even(user_inputs):
    for num in user_inputs:
        if num % 2 == 0:
            return False
    return True
is_even(user_inputs)
#     return (user_inputs )
# print(is_even(user_inputs))    
# is_even(user_input)